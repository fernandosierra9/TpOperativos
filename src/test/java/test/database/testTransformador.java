package test.database;

import dominio.cargarCliente.ClienteManager;
import dominio.transformador.TransformadorManager;
import org.junit.Test;
import org.uqbarproject.jpa.java8.extras.PerThreadEntityManagers;

import javax.persistence.EntityManager;

public class testTransformador {

    @Test
    public void testCargarTransoformador() {
        TransformadorManager transformadorManager = new TransformadorManager();
   //     transformadorManager.persistirCliente();
        //clienteManager.persistirDispositivos();
        //entityManager().clear();
    //    EntityManager entityManager = PerThreadEntityManagers.getEntityManager();
    }


}
